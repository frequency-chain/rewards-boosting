<script lang="ts">let { tag = 'div', bold = false, children, ...rest } = $props();
let isBold = $derived(bold ? 'font-bold	' : '');
export {};
</script>

<svelte:element
  this={tag === 'h0' ? 'h1' : tag}
  {...rest}
  class={`${tag === 'h0' ? 'h0' : ''} ${isBold} ${rest.class ?? ''}`}
>
  {@render children?.()}
</svelte:element>
