<script lang="ts">import { Root, Item, Trigger, Content } from '../shadcnComponents/ui/accordion';
let { trigger, content, intent = 'dark' } = $props();
</script>

<Root>
  <Item value="item-1" class={intent === 'light' ? 'text-white' : 'text-black'}>
    <Trigger {intent}>{trigger}</Trigger>
    <Content
      >{#if typeof content === 'function'}
        {@render content()}
      {:else}
        {content}
      {/if}</Content
    >
  </Item>
</Root>
