<script lang="ts">import FormElement from './FormElement.svelte';
import { formItem } from '../styles/formItemStyles';
let { label = '', isRequired = false, description = '', value = $bindable(''), placeholder = 'Enter some text...', error, disabled, ...rest } = $props();
</script>

<FormElement {label} {isRequired} {description} {error}>
  <input
    {...rest}
    class={formItem({ error: !!error, height: 'sm', class: rest.class })}
    type="text"
    {placeholder}
    {disabled}
    bind:value
  />
</FormElement>
