<script lang="ts">import FormElement from './FormElement.svelte';
import { formItem } from '../styles/formItemStyles';
let { label = '', isRequired = false, description = '', value = $bindable(''), placeholder = 'Enter some text...', error, rows = 5, ...rest } = $props();
</script>

<FormElement {label} {isRequired} {description} {error}>
  <textarea
    {...rest}
    class={formItem({
      error: !!error,
      height: rows ? 'auto' : 'sm',
      class: rest.class,
    })}
    {rows}
    {placeholder}
    bind:value
  ></textarea>
</FormElement>
